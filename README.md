# Stanley
A decoupled, object-oriented restaurant management and reservation engine featuring a dual-portal console (Staff Admin & Customer Client). Engineered with abstract base lifecycles, strict business logic constraint validation, and a date-indexed hash map scheduling engine optimizing lookups to $O(1)$ constant time.
